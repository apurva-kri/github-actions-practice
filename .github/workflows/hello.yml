name: greet-hello

on:
  push:

jobs:
  build:
    runs-on: ubuntu:latest

    steps:
      - name: code-checkout
        uses: actions/checkout@v4

      - name: greet-user
        run: echo "Hello from GitHub Actions!"
